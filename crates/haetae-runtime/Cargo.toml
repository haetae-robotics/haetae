[package]
name = "haetae-runtime"
description = "Haetae runtime: transport-agnostic gate loop with freshness, replay detection, trusted time and incident recording"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
haetae-core.workspace = true
sillok.workspace = true
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"

[dev-dependencies]
tempfile = "3.27.0"
